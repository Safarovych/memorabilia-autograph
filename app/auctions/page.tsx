import Link from 'next/link';import PageShell from '../../components/PageShell';import{auctions}from'../../lib/catalog';export default function Auctions(){return <PageShell><main className="auctionSection"><div className="sectionHead"><div><p className="eyebrow">LIVE / AUCTIONS</p><h2>SIGNED LEGENDS</h2><p>Rare football pieces with a story.</p></div></div><div className="auctionGrid">{auctions.map(a=><article className="auctionCard" key={a.slug}><Link href={`/auction/${a.slug}`} className={`auctionVisual ${a.tone}`}><span className="liveBadge">LIVE</span><span className="auctionName">{a.player.toUpperCase()}</span><span className="auctionNumber">{a.mark}</span></Link><div className="auctionInfo"><h3>{a.player}</h3><p>{a.item} • {a.bids} bids</p><div className="bidRow"><div><span>CURRENT BID</span><strong>€{a.bid.toLocaleString('de-DE')}</strong></div><div><span>ENDS</span><strong>{new Date(a.endsAt).toLocaleDateString('en-GB')}</strong></div></div><Link className="bidBtn" href={`/auction/${a.slug}`}>VIEW LOT</Link></div></article>)}</div></main></PageShell>}
