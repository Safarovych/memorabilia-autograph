import Link from 'next/link';import{notFound}from'next/navigation';import PageShell from '../../../components/PageShell';import{auctions}from'../../../lib/catalog';export default async function AuctionPage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const a=auctions.find(x=>x.slug===slug);if(!a)notFound();return <PageShell><main className="section"><div style={{display:'grid',gridTemplateColumns:'1fr 1fr',gap:30}}><div className={`auctionVisual ${a.tone}`} style={{height:560}}><span className="liveBadge">LIVE</span><span className="auctionName">{a.player.toUpperCase()}</span><span className="auctionNumber">{a.mark}</span></div><div><p className="eyebrow">SIGNED MEMORABILIA / LIVE AUCTION</p><h1 style={{font:'normal 52px Georgia,serif'}}>{a.player}</h1><p style={{color:'#9fa3aa',lineHeight:1.8}}>{a.item}. {a.description}</p><div style={{borderTop:'1px solid var(--line)',borderBottom:'1px solid var(--line)',padding:'22px 0',margin:'25px 0'}}><span className="eyebrow">CURRENT BID</span><div style={{font:'38px Georgia,serif'}}>€{a.bid.toLocaleString('de-DE')}</div><p>{a.bids} bids • ends {new Date(a.endsAt).toLocaleString('en-GB')}</p></div><Link className="btn" href="/login">PLACE BID</Link></div></div></main></PageShell>}
