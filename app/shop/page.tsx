import Link from 'next/link';import PageShell from '../../components/PageShell';import{products}from'../../lib/catalog';export default function Shop(){return <PageShell><main className="section"><div className="sectionHead"><div><p className="eyebrow">SHOP / COLLECTION</p><h2>CURATED FOOTBALL</h2><p>Premium jerseys, retro pieces and signed collectibles.</p></div></div><div className="productGrid">{products.map(p=><article className="productCard" key={p.slug}><Link href={p.signed?`/auction/${p.slug}`:`/product/${p.slug}`} className={`productVisual ${p.tone}`}><span className="badge">{p.badge}</span><span className="shirtMark">{p.mark}</span><span className="shirtNumber">{p.signed?'10':'11'}</span></Link><div className="productInfo"><div><h3>{p.name}</h3><p>{p.meta}</p></div><strong>{p.signed?'AUCTION':`€${p.price.toFixed(2)}`}</strong></div></article>)}</div></main></PageShell>}
