import Link from 'next/link';import{notFound}from'next/navigation';import PageShell from '../../../components/PageShell';import{products}from'../../../lib/catalog';export default async function ProductPage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const p=products.find(x=>x.slug===slug&&!x.signed);if(!p)notFound();return <PageShell><main className="section"><div style={{display:'grid',gridTemplateColumns:'1fr 1fr',gap:30}}><div className={`productVisual ${p.tone}`} style={{height:520}}><span className="shirtMark">{p.mark}</span><span className="shirtNumber">11</span></div><div><p className="eyebrow">PRODUCT</p><h1 style={{font:'normal 52px Georgia,serif'}}>{p.name}</h1><p className="lead">{p.description}</p><h2>€{p.price.toFixed(2)}</h2><p style={{color:'#777'}}>Sizes: {p.sizes?.join(' / ')||'One size'}</p><Link className="btn" href="/cart">ADD TO BAG</Link></div></div></main></PageShell>}
